//User comments page

//A page that dynamically renders with all of this users comments. 
//The comments should show the comment, the time, the post (with link), and stance (for/against)

//Function for calling users comments
/* const getComments = () => {
    //Make an api call for the comments
        //file structure: user -> comments
    //returns json list of all comments to be rendered    
}; */ 